## GitHub Markdown compatibility patch

This patch release fixes README rendering on GitHub without changing the formalized mathematics.

### Changes

- replace unsupported `\operatorname{...}` uses in displayed formulas with GitHub-compatible `\mathrm{...}`;
- restore rendering of the finite camera equivalence and the unequal-window embedding;
- align `CITATION.cff` and `.zenodo.json` with version `0.2.1` and release date `2026-08-16`.

### Verification

- no Lean source, theorem statement, proof, or public API changed;
- the public root, explicit axiom audit, and sexagesimal examples are rebuilt with warnings treated as errors before publication;
- the release tag targets the exact commit validated by the release workflow.
